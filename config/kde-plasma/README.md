## KDE Plasma Configuration
Contains **CLEAN** kde plasma configuration which can be linked

The files are cleaned using the `clean.py` script which removes known state from the files allowing it to be version controlled
